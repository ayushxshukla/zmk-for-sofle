---
include:
  # Displays are compiled out, so only the plain sofle_left / sofle_right
  # variants are needed. The nice_view builds have been removed.
  - board: nice_nano//zmk
    shield: sofle_left
    snippet: studio-rpc-usb-uart
  - board: nice_nano//zmk
    shield: sofle_right

  # Emergency "forget all Bluetooth pairings & settings" firmware
  - board: nice_nano//zmk
    shield: settings_reset
